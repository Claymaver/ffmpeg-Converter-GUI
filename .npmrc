# Prevent optional dependencies from failing build
optional=true

# Increase network timeout for FFmpeg download
fetch-timeout=60000
fetch-retries=5

# Use faster registry
registry=https://registry.npmjs.org/
